## Vie privée / Privacy — station telemetry

This site is **fully self-hosted**. There are no cookies, no third-party
scripts, no ad networks, and Google Analytics was removed in 2026.

Instead, the site runs its **own first-party telemetry**, described here in
full:

**What is collected (per visit):**
- pages viewed, referrer origin, device class (mobile/tablet/desktop)
- click positions (normalized, for heatmaps), maximum scroll depth, time on page
- outbound link targets

**Who is identified:** a pseudonymous random ID generated by your browser and
stored in `localStorage` — **not** a cookie, not shared with anyone, not linked
to your name. Your IP is stored only as a truncated hash for abuse-rate-limiting.
No cross-site tracking, no fingerprinting beyond this ID, no advertising use,
no data sale, no third parties ever.

**Why:** understanding which modules are useful (hot/cold spots), which pages
need work, and how the research is being read.

**Where it lives:** one SQLite database on the operator's own server. Retention:
raw events **90 days**, aggregated visitor rows **400 days**, then deleted.

**Your controls:**
- open the console (`~` / backtick) and type `telemetry off` — tracking stops
  immediately for this browser and your profile is wiped
- `clear` wipes the local profile at any time
- `telemetry on` re-enables it
- browsers set to Do-Not-Track are honored automatically

**Legal basis:** legitimate interest of the site operator (measure its own
website), processed under GDPR art. 6(1)(f) with the balancing interests and
opt-out documented above. Data controller: Jonas Mehtali, Strasbourg.
